#ifndef searchhdr
#define searchhdr

#include <stdio.h>
#include <stdlib.h>

void print_array(int *array, size_t start, size_t end);
int binary_search(int *array, size_t size, int value);

#endif /* searchhdr */
